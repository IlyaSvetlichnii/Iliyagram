<nav class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Tape', root_path, class: 'navbar-brand' %>
      <%= link_to 'My page', profile_path(current_user.name), class: 'navbar-brand' %>

    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

     
        <% if user_signed_in? %>
         <ul class="navbar-nav navbar-right">
          <li>
            <a href="<%= new_post_path %>">
                <button type="button" class="btn btn-default">New post</button>
            </a>
          </li>
        </ul>
        <ul class="navbar-nav navbar-right">
        <p class="navbar-text">
        Hello, <%= link_to current_user.name, edit_user_registration_path(current_user), class: "navbar-link"%>
        </p>
        <ul class="navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" arai-haspopup="true" arai-expanded="false">Account <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to 'Edit profile', edit_user_registration_path(current_user)%>
              </li>
              <li>
                <a href="#">View orders</a>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to 'Sing Out', destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        </ul>
        <% else %>
          <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-default navbar-btn'%>
          <%= link_to 'Login in', new_user_session_path, class: 'btn btn-default navbar-btn'%>
        <% end %>
      </ul>
    </div>
  </div>
</nav>